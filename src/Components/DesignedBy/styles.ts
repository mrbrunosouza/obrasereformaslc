import styled from "styled-components";

export const Container = styled.section`
  color: #666;
  text-transform: uppercase;
  font-size: 0.8rem;

  background: #000;

  div {
    padding: 1.5rem 0;
    width: 1280px;
    margin: 0 auto;
  }
`